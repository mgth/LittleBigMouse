[package]
name = "lbm-layout"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "The LittleBigMouse layout model: monitors in millimetres and pixels, placement solvers, zones"

[dev-dependencies]
serde_json = "1"
