[package]
name = "lbm-zones"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "The LittleBigMouse zone graph: zones, links between their edges, the ZonesLayout XML form"

[dependencies]
lbm-geom = { path = "../lbm-geom" }
roxmltree.workspace = true
slotmap.workspace = true
